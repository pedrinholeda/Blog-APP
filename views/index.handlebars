<div class="jumbotron">
  <h1 class="display-4">Bem-vindo ao Blog do Node</h1>
  <p class="lead"> Blog de Tecnologia, desenvolvido com Node.js + Express + MongoDB</p>
  <hr class="my-4">
  <p>Aqui você pode ler sobre o mundo da computação</p>
  <a class="btn btn-outline-primary" href="#" role="button">Crie uma Conta</a>
</div>

<hr>
<h2>Postagens Recentes</h2>

{{#each postagens}}
<div class="card mt-4">
  <div class="card-body">
    <h3>{{titulo}}</h3>
    <p>{{descricao}}</p>
    <a href="/postagem/{{slug}}"><button type="button" class="btn btn-outline-dark">Leia Mais</button></a>
    <hr>
    <small>{{categoria.nome}}</small>
    <br>
    <small>Data de publicação: {{data}}</small>
  </div>
</div>
{{else}}
<h5>Nenhuma postagem</h5>
{{/each}}